

import sys
sys.path.insert(0, '../')
cities = import all_the_cities
kdbush = import kdbush
geokdbush = import ../

print("=== geokdbush benchmark ===")

n = cities.length
k = 1000

random = []
for i in range(0,k,1):
  randomPoints.push({
    lon: -180 + 360 * Math.random(),
    lat: -60 + 140 * Math.random()
})
